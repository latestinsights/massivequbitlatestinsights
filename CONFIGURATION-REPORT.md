# Weekly configuration verification — 13 September 2026

- Scheduler moved to GitHub-hosted Actions at Sunday 21:30 Australia/Melbourne using GitHub's timezone-aware schedule. It follows daylight saving and runs without the local computer. The old local Codex schedule and unpublished workflow copy were removed.
- Content repository guard and validation configuration published. GitHub [validation passed](https://github.com/latestinsights/massivequbitlatestinsights/actions/runs/34753350748).
- Website integration deployed by the unchanged [Azure pipeline](https://github.com/massivequbitio/massivequbitioweb/actions/runs/34753209769); deployment passed. Live browser inspection confirmed all six existing cards show concierge@massivequbit.io, MassiveQubit logo imagery and real Read More links. All six destination URLs, the logo and feed CORS passed a separate read-only network check.
- Five local tests passed: malformed/unsafe feed rejection; retired queue refusal without feed writes; Sydney daylight-saving and year/week boundaries; no-write preparation and duplicate-week refusal; invalid source records, changed articles, wrong metadata and stale reviews refused. Content CI also executes the publication gate tests. Production Vite build passed.
- Publication boundary: new Markdown, evidence review, entries, feed and week ledger in one normal Git push. A rejected or ambiguous push must reconcile remote state before retry. No force pushes. At most one new article per Monday–Sunday Sydney calendar week. All existing IDs are protected from new-article overwrite; the six separate full drafts remain pending approval.
- Credentials verified: Git push access to both repositories; existing AZURE_STATIC_WEB_APPS_API_TOKEN_VICTORIOUS_DUNE_07452BF10 secret name present and deployment successful. The content repository currently has no OPENAI_API_KEY secret, so future cloud research runs are configured but credential-blocked. No secret values were read or committed.
- Cloud dry run 34754227821 reached the GitHub runner with the repository checked out, then failed closed at the explicit credential preflight because OPENAI_API_KEY is absent. It performed no research, writes or publication.

## Exact scope of testing

Configuration dry runs, a live-research/editorial dry run, and live synchronization checks passed. The local research candidate contains 1,126 prose words, three verified sources (two peer-reviewed papers and the official FMI 3.0.2 specification), a seven-minute finished-text reading estimate, and a claim/source review. Its no-write gate used a simulated Sunday 22:17 Sydney timestamp; the production command always uses wall-clock time. No article or pending full draft was published. An unattended scheduled research-to-publication run has NOT been exercised. The schedule is enabled and website integration is live; end-to-end unattended publication is not yet proven operational. Source authenticity, claim support and feasibility are substantive editorial checks assigned to the research task, not truths established by passing the synthetic mechanical fixtures.

Local execution requires this computer awake, Codex running and signed in with available usage, network access and valid Git credentials. Sandbox/network approval may still require attention during an unattended run; prior successful interactive pushes do not establish unattended permission. The task must record an actionable failure and publish nothing if these conditions or editorial checks fail. Changing the host timezone requires reconfiguring the schedule; the publication gate always uses Australia/Sydney and will refuse publication outside its window.

Local dry-run artifacts: weekly-runs/dry-run-article.md, weekly-runs/dry-run-candidate.json and weekly-runs/dry-run-result.json. Procedure: [WEEKLY-RESEARCH.md](WEEKLY-RESEARCH.md). Tests: `node --test tools/insights.test.mjs tools/publication-gate.test.mjs`; live check: `node tools/check-live-insights.mjs`.
